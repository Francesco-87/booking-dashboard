package com.ciccone.backend.entity;

// Enum defining user roles in the system
public enum UserRole {
    // Administrator role with full system access and permissions
    ADMIN,
    // Staff member role for managing bookings and services
    STAFF,
    // Customer role for booking services
    CUSTOMER
}